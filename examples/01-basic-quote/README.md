# Example 1: Basic quote with mock ERP

No ERP system required. Runs against the built-in mock backend with 200 electronics parts.

```bash
ANTHROPIC_API_KEY=your_key python examples/01-basic-quote/basic_quote.py
```
